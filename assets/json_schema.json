{
    "type": "object",
    "properties": {
      "tokens": {
        "type": "array",
        "items": {
          "type": "array",
          "description": "Token array. For words: [\"word\", input, ipa, output]. For whitespace/punctuation: [\"whitespace\"|\"punctuation\", value].",
          "prefixItems": [
            {
              "enum": [
                "word",
                "whitespace",
                "punctuation"
              ]
            },
            {
              "type": "string",
              "description": "For 'word': original input word. For 'whitespace'/'punctuation': the value (e.g., \" \", \"，\", \"?\")."
            },
            {
              "type": "string",
              "description": "IPA transcription (only present for 'word' tokens)."
            },
            {
              "type": "string",
              "description": "Transliteration output (only present for 'word' tokens)."
            }
          ],
          "items": {
            "type": "string"
          },
          "minItems": 2,
          "maxItems": 4
        }
      }
    },
    "required": [
      "tokens"
    ],
    "additionalProperties": false
  }
